{
  "name": "jackhaben.com",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview", 
    "deploy": "aws --region us-east-2 --profile vue-deployer s3 sync ./dist s3://jack-haben --delete" 

  },
  "dependencies": {
    "vue": "^3.2.41"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^3.1.2",
    "vite": "^3.1.8"
  }
}
